# design/ is a working folder, not its own repo. If a parent repo tracks it, keep the machinery
# out; the minted prereg .docx (the committed preregistration) is the deliverable this stage ships.
rayleigh.yaml
designdocs/experiments.yaml
archive/
__pycache__/
*.pyc
