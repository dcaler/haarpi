# {{PROJECT}} — Executable experiments (cycle {{CYCLE}})

*Status: DRAFT — authored during `rayleigh plan` (Cale + Claude) · created {{DATE}}*
*Human-facing companion to the executable spec [`experiments.yaml`](./experiments.yaml). These
experiments fulfil the committed analytical framework (`design/designdocs/EXPERIMENTS.md`) using the
raster-built tooling. The results write-up is delivered separately as the datestamped `.docx`.*

> Framework to fulfil: `design/…_prereg` (research questions + analytical approach).
> Run against: `{{CODE_PATH}}` (package `{{PACKAGE}}`).

<!--
  PLAN SESSION: this is a skeleton. Fill it IN COLLABORATION WITH CALE (see PLANNING.md), tracing
  every experiment to a prereg question. Each experiment maps 1:1 to an entry under
  experiments.yaml `experiments:`. Keep it honest — question, metric, and expected direction are
  fixed BEFORE data. Delete these comments as you go.
-->

## The codebase under test

<!-- What the built code/ provides: the single-run entrypoint conduct calls (the run_adapter), the
     config knobs the experiments vary, and the output format each run produces. -->

## Experiments

<!-- One subsection per experiment, mirroring experiments.yaml. For each: the prereg question it
     answers, the design (what varies + seeds), the metric, the expected direction, the planned
     outputs, and mode (confirmatory/exploratory). -->

### E1 — (title)

- **Fulfils prereg question:** …
- **Mode:** confirmatory | exploratory
- **Design:** … (axes / conditions, seeds)
- **Metric:** …
- **Expected direction:** …
- **Planned outputs:** …

## Notes & open decisions

<!-- Dependencies between experiments, compute caveats surfaced to Cale, validity boundaries. -->
