# {{PROJECT}} — Experiment design / preregistration (cycle {{CYCLE}})

*Status: DRAFT — authored during `rayleigh init` (Cale + Claude) · created {{DATE}}*
*This is the DESIGN DOC: the targeted research questions and the analytical approach that
answers them. It is rendered to the prereg `.docx` you commit with `haarpi next` — which locks
the preregistration and hands off to raster to build the data infrastructure the approach needs.*

> Research brief (from `rayleigh init`): {{BRIEF}}

<!--
  DESIGN SESSION: this is a skeleton. Fill it IN COLLABORATION WITH CALE (see PLANNING.md),
  grounded in the minted litReview + the brief. You are UPSTREAM of the code — you SPECIFY the
  analytical approach; raster builds the code/data infrastructure afterwards. Keep it honest:
  the questions and the analytical approach are fixed BEFORE any data (or code) exists. Delete
  these comments as you go.
-->

## Research questions

<!-- The targeted questions this cycle answers. For each: the question, why it matters (name the
     litReview prior it rests on), and what would confirm or disconfirm it. -->

### Q1 — (short label)
- **Question:** …
- **Grounding (prior):** … (the litReview finding this rests on)
- **Confirms / disconfirms:** …

## Analytical approach

<!-- HOW each question is answered: the method(s), the estimand, the identification/inference
     strategy, the comparisons. This is the intellectual core — it does NOT assume finished code;
     it SPECIFIES what the analysis must do. -->

### For Q1
- **Method / estimand:** …
- **Inference:** … (what makes the answer credible)

## Data infrastructure required (the handoff to raster)

<!-- What the analytical approach needs raster to BUILD: the dataset(s) / model, the parameters
     the analysis varies, the outputs each run must produce, and the single-run entrypoint
     contract. This section is the build spec raster reads once the prereg mints. Be concrete. -->

## Notes & open decisions

<!-- Anything deferred, dependencies between questions, and every scope/compute caveat you
     surfaced to Cale. -->
